/**
 * @author dingkai
 */
;(function(){
    $.extend($.fn,{
       zoom: function(options){
           var defaults;
           defaults = {
               obj:null,
               src:null
           };
           
           
       } 
    });
})();
